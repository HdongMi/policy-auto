steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: |
          npm init -y
          npm install playwright node-fetch cheerio  # 👈 여기에 playwright 추가

      - name: Install Playwright Browsers
        run: npx playwright install chromium --with-deps  # 👈 브라우저를 설치하는 '새로운 단계'

      - name: Run crawlers
        run: |
          node crawler/mss.js
          node crawler/bizinfo.js
          node crawler/merge.js

      - name: Commit result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "자동 정책 업데이트" || exit 0
          git push
